[Interface]
Address = 10.8.0.1/24
ListenPort = 51820
PrivateKey = GERMANY_NODE_PRIVATE_KEY

# Enable forwarding/NAT on the VPS separately. Example Linux rules:
# sysctl -w net.ipv4.ip_forward=1
# iptables -t nat -A POSTROUTING -s 10.8.0.0/24 -o eth0 -j MASQUERADE
